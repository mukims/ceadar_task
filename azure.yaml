# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: rag-pipeline
metadata:
    template: azd-init@1.23.2
services:
    rag-pipeline:
        project: .
        host: containerapp
        language: python
        docker:
            path: Dockerfile
resources:
    rag-pipeline:
        type: host.containerapp
        port: 8501
